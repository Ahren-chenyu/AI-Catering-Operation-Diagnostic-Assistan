# AI 餐饮经营诊断助手｜Product Specification

**版本**：v1.1（与当前代码实现对齐）  
**产品形态**：Web MVP / 经营决策 Agent Demo

---

## 1. 产品定位

### 产品名称

AI 餐饮经营诊断助手（界面品牌名：AI餐饮经营助手）

### 一句话定位

让餐饮经营者不用自己分析复杂报表，也能知道门店发生了什么、为什么发生，以及下一步应该做什么。

### 核心价值

传统餐饮 SaaS 主要负责记录、统计和展示经营数据。

本产品在 SaaS 数据之上增加 **经营决策层**：

```text
数据记录
  → 数据统计
  → AI / 规则主动发现异常
  → 分层原因分析（附证据）
  → 生成具体行动计划
  → 执行后自动复盘
```

与「聊天机器人」的区别：用户无需组织专业问题；系统围绕「营业额异常诊断」完成一条可验证的业务闭环。

---

## 2. 目标用户

### 核心用户

中小型餐饮门店老板、店长、区域/连锁运营人员。

典型特征：

- 有一定餐饮经营经验
- 已接触或使用餐饮 SaaS / 报表
- 能看到营业额、订单等数据，但不一定具备数据分析能力
- 更关心「店里到底出了什么问题」和「我下一步应该做什么」
- 没有时间每天研究复杂报表

### 用户每天希望快速知道的事

1. 今天经营情况是否正常？  
2. 有没有值得关注的问题？  
3. 如果有问题，为什么？依据是什么？  
4. 我现在应该做什么？  
5. 上一轮动作做得怎么样？

---

## 3. 核心用户任务（JTBD）

当门店经营指标出现异常时，我希望系统自动告诉我：

- 发生了什么  
- 为什么发生  
- 有什么数据证据  
- 下一步应该做什么  
- 执行一段时间后效果如何  

从而减少自己分析报表和寻找原因的时间，把精力用于经营、营销和产品提升。

---

## 4. 核心产品问题

传统工具可以告诉老板：「营业额是多少。」

但往往回答不好：「为什么下降？」以及「接下来做什么？」

本产品重点解决：

> **营业额出现异常后，自动完成经营诊断、给出可执行行动方案，并在行动周期结束后复盘。**

---

## 5. 唯一核心用户路径

```text
老板进入系统（默认进入 AI 自动汇报）
        ↓
系统检查经营数据并对比历史基线
        ↓
发现营业额异常（或展示正常状态说明）
        ↓
进入 AI 经营分析：分层拆解 + 数据证据 + DeepSeek 解读
        ↓
进入 AI 行动指南：具体方案（步骤 / 周期 / 预算 / 指标）
        ↓
老板确认并「启动行动」
        ↓
AI 复盘记录中跟踪轮次，到期后基于区间数据自动复盘
```

侧边栏导航对应：

| 顺序 | 页面 | 路径 |
|------|------|------|
| 1 | AI自动汇报 | `/dashboard` |
| 2 | AI经营分析 | `/diagnosis` |
| 3 | AI行动指南 | `/action-plan` |
| 4 | AI复盘记录 | `/review-records` |

---

## 6. MVP 范围

### 已实现范围

- 单门店演示（默认 `store-001` / XX烧烤店）
- 规则引擎诊断 + DeepSeek 解读 / 行动 / 复盘
- Supabase 经营数据与诊断快照（可选，失败则 Mock）
- 四页核心路径完整可走通
- 未配置 AI Key 时规则引擎行动方案降级可用

### 明确不做（当前版本）

- 登录注册与账号体系  
- 多门店权限与复杂后台  
- 真实支付、真实微信 / POS 连接  
- 完整 CRM 与营销投放系统  
- 复杂自定义报表与多角色权限  

---

## 7. 页面规格

### Page 1：AI自动汇报（`/dashboard`）

**作用**：打开系统后第一时间知道今日概况与是否异常。

**内容**：

- 今日经营概览：营业额、订单量、客单价、新客、老客、历史同期营业额均值  
- AI 异常提醒：基于规则引擎生成的 insight 卡片  
- CTA：查看 AI 诊断 → `/diagnosis`

### Page 2：AI经营分析（`/diagnosis`）

**作用**：回答「为什么营业额下降（或波动）？」

**内容结构**：

1. **AI 经营解读**（DeepSeek，基于 DiagnosisResult；可会话缓存 / 日快照）  
2. **核心指标变化**  
3. **当前问题** 与 **综合诊断结论（规则引擎）**  
4. **数据证据 / 数据事实 / 规则推断**  
5. **诊断逻辑**（分层步骤卡片）  
6. **当前无法判断**（unknownFactors，如营销、天气、竞品数据缺失）  
7. CTA：查看行动计划 → `/action-plan`

**分层分析逻辑（规则引擎）**：

```text
第一层：营业额
        ↓
第二层：订单量 vs 客单价（谁是主因）
        ↓
第三层：若订单量相关 → 新客 vs 老客
```

数据层另有渠道 / 餐段 / 菜品等演示拆解字段，当前诊断主路径以营业额—订单—客群为主。

### Page 3：AI行动指南（`/action-plan`）

**作用**：回答「现在应该怎么办？」

**要求**：

- 建议必须具体、可执行  
- 禁止仅输出「加强营销 / 提高复购 / 优化产品」等空话  
- 尽量包含：做什么、为什么、执行周期、建议预算、目标、核心指标、复盘指标  
- 预算与预测数字须标注为「AI测算值」（非系统内事实时）

**数据来源**：

- 优先：DeepSeek 基于诊断结果生成的 `recommendedAction`  
- 回退：`generateActionPlan` 规则模板  

**交互**：支持「启动行动」，写入本地行动轮次（`localStorage`）。

### Page 4：AI复盘记录（`/review-records`）

**作用**：跟踪每轮行动，到期后自动评估效果。

**流程**：

1. 展示该门店行动轮次列表（进行中 / 无数据 / 已完成）  
2. 到达结束日期后，调用 `/api/action-review`  
3. 拉取行动区间内的日经营数据；无数据则标记 `no_data`  
4. 有数据时由 DeepSeek 生成：总结、指标分析、目标评估、下一步建议  

---

## 8. AI 证据原则

所有经营判断必须有数据依据。AI **不得**基于系统不存在的数据做事实性断言。

示例：系统没有营销数据时，不能说「营销活动效果下降导致营业额下降」，而应说明「当前缺少营销活动数据，无法判断」。

必须区分：

| 类型 | 含义 |
|------|------|
| 已知事实 | 系统内原始或可计算指标 |
| 数据推断 | 基于变化率比较得出的结论 |
| 无法判断 | unknownFactors 中列明的缺失维度 |

DeepSeek Prompt 层强制：只能使用输入的 DiagnosisResult；不得把 unknownFactors 说成确定原因。

---

## 9. 异常检测规则

MVP 采用简化规则（`DEMO_ANOMALY_THRESHOLD = 10`）：

- 变化率 = `(当期 − 历史基线) / 历史基线 × 100%`  
- 营业额相对历史同期均值偏离超过 **10%** 时，视为异常并触发提醒  

说明：10% 为 Demo 参数，不代表生产最终算法。

未来可扩展：星期/节假日、季节、天气、营销活动、门店历史波动、同行业 Benchmark 等。

显著变化辅助阈值：`DEMO_SIGNIFICANT_CHANGE_THRESHOLD = 5`（用于拆解主因判断）。

---

## 10. 示例业务数据

门店：XX烧烤店（`store-001`，类型：烧烤）

| 指标 | 当日 | 历史基线均值（约） |
|------|------|-------------------|
| 营业额 | ¥28,000 | ¥33,600 |
| 订单量 | 1,200 | 1,445 |
| 客单价 | ¥80 | ¥70.8 |
| 新客 | 230 | 403 |
| 老客 | 500 | 901 |

规则引擎典型判断：

- 营业额下降，主要观察方向为 **订单量下降**（客单价上升）  
- 订单侧进一步指向 **新客下降** 为主因观察方向  

数据来源：`lib/data/mockData.ts`；配置 Supabase 后优先读库，失败回退 Mock。

---

## 11. 数据模型

### Store

- `id` · `name` · `type`

### RawDailyMetrics / DailyMetrics

- 日期、营业额、订单量、客单价、新客、老客  
- DailyMetrics 另含各指标相对基线的变化率  

### HistoricalBaseline

- 各指标历史同期均值（用于变化率与异常判断）

### AIInsight

- `type`：anomaly / judgment / evidence / unknown  
- `title` · `description` · `evidence[]` · `severity`

### DiagnosisResult（规则引擎输出）

- `status`：normal / anomaly  
- `currentProblem` · `primaryCause` · `customerCause` · `summary`  
- `facts` · `inferences` · `steps` · `insights` · `unknownFactors`

### AIInsightResult（DeepSeek）

- `summary` · `reasoning` · `recommendedAction`  
  （含 title、objective、steps、duration、budget、targetMetric、reviewMetrics）

### ActionPlanViewModel

- 页面展示模型；`source` 为 `deepseek` 或 `rules`

### ActionRoundRecord / ActionReviewResult

- 行动轮次：开始/结束时间、计划快照、复盘状态与结果  
- 复盘：summary、metricAnalysis、goalAssessment、nextSteps  

### 诊断快照

- 按门店 + 日期缓存 DiagnosisResult + AIInsightResult，避免重复调用大模型  

---

## 12. Agent 概念与工具

本产品不是开放域聊天机器人，而是围绕「营业额异常诊断」的任务型 Agent。

```text
读取经营数据
  → 判断营业额是否异常
  → 分析订单量 / 客单价
  → 分析新客 / 老客
  → 生成有证据的诊断（规则引擎）
  → 生成经营解读与行动计划（DeepSeek，可降级）
  → 行动到期后拉取区间数据并复盘（DeepSeek）
```

### 当前实现的能力模块

| 模块 | 职责 |
|------|------|
| `getRevenueMetrics` | 营业额、变化、基线、是否异常 |
| `getOrderMetrics` | 订单量及变化 |
| `getCustomerMetrics` | 新客 / 老客及变化 |
| `runDiagnosis` | 完整 DiagnosisResult |
| `generateAIInsight` | DeepSeek 解读与推荐行动 |
| `generateActionPlan` / `resolveActionPlanView` | 规则行动方案与页面视图合并 |
| `generateActionReview` | 行动周期复盘 |

数据访问经 `dataProvider`：Supabase 优先，超时或未配置则 Mock。

---

## 13. 系统架构（实现视角）

```text
展示层（App Router 页面 + 组件）
        ↓
API（dashboard / diagnosis / action-plan / action-review）
        ↓
业务服务（businessContext · pageData · dailyDiagnosisSnapshot）
        ↓
规则诊断引擎 ←→ DeepSeek（洞察 / 行动 / 复盘）
        ↓
dataProvider → Supabase 或 mockData
```

技术栈：Next.js 15、React 19、TypeScript、Tailwind CSS、Supabase、DeepSeek（OpenAI 兼容 SDK）。部署支持腾讯云 CloudBase CloudRun（standalone）与 Vercel。

---

## 14. 产品设计原则

1. **主动而不是被动** — 老板无需先提问  
2. **证据优先** — 无依据不下结论  
3. **结论必须可解释** — 分层步骤与证据对用户可见  
4. **建议必须可执行** — 含步骤、周期与观察指标  
5. **先解决一个问题** — MVP 聚焦「营业额异常以后怎么办」  
6. **可降级** — 无云端配置时仍可用 Mock + 规则引擎演示闭环  

---

## 15. UI 设计方向

- 专业、简洁、餐饮经营管理感；数据驾驶舱 + AI 助手感  
- 不做聊天机器人主界面  
- 首页突出：营业额、异常提醒、进入诊断与行动  
- 色彩：中性色为主；红=异常/下降，绿=正常/增长，黄=需关注  
- 支持桌面侧边栏 + 移动端抽屉导航  

---

## 16. 验收标准

用户打开网站后应能：

1. 看到一家模拟餐饮门店（或已接入的 Supabase 门店）  
2. 看到今日营业额及关键指标  
3. 在异常场景下看到 AI 主动提醒  
4. 进入诊断页，看到规则结论、分层逻辑与数据证据  
5. 看到 DeepSeek 经营解读（若已配置 Key）或明确的加载/降级表现  
6. 看到行动计划（含周期、预算、目标、复盘指标）  
7. 能够启动行动，并在复盘页看到轮次记录  
8. 核心流程无需用户输入自然语言问题即可完成  

核心路径必须完整可运行：

```text
AI自动汇报 → AI经营分析 → AI行动指南 → AI复盘记录
```

---

## 17. 版本与演进

### v1.1（当前）

四页闭环、规则引擎 + DeepSeek、Supabase/Mock 双轨、诊断快照、行动复盘。

### 后续候选

- 历史趋势与可视化  
- 渠道 / 餐段 / 菜品拆解进入主诊断路径  
- 多门店、门店画像、行业 Benchmark  
- 定时监控与主动推送（从「打开再诊断」到「AI 主动找上门」）  

---

## 18. 文档关系

- **本文件**：产品目标、范围、页面与规则、验收标准  
- **README.md**：面向开发者的简介、架构、本地运行与部署说明  
